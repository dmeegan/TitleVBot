
// form.addEventListener('submit', sasNetworkLayout)

function sasNetworkLayout() {
const form = document.getElementById('NetworkLayoutForm')

const designFlow = document.getElementById('UserInput-designFlow').value
const trenchWidth = document.getElementById('UserInput-trenchWidth').value
const trenchHeight = document.getElementById('UserInput-trenchHeight').val
const trenchLength = document.getElementById('UserInput-trenchLength')
const reserveAreaBetweenTrenches = document.getElementById('UserInput-reserveAreaBetweenTrenches')
const percRate = document.getElementById('UserInput-percRate')
const soilClass = document.getElementById('UserInput-soilClass')

alert(designFlow)
};